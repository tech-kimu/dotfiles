export GEM_HOME=$(gem env user_gemhome)
export PATH=$PATH:$GEM_HOME/bin:$HOME/Script
export EDITOR='nvim'

[[ -f ~/.bashrc ]] && . ~/.bashrc

[[ $XDG_SESSION_TYPE == "tty" ]] && start-hyprland &> /dev/null
